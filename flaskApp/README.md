# Ma Boutique - Flask + MongoDB

## Installation
1. Cloner le dépôt dans `boutique-flask/`.
2. Créer un virtualenv et installer les dépendances :
   ```bash
   python -m venv venv
   source venv/bin/activate   # Windows: venv\Scripts\activate
   pip install -r requirements.txt
